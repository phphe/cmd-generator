{"name":"pkill","desc":"The pkill command is used to terminate processes based on process name or other attributes.","usages":[{"form":{"":{"name":"Process name","required":true,"type":"string"},"-a":{"type":"check","name":"Ignore the user who owns the process and terminate all processes","optionName":"-a"},"-f":{"type":"check","name":"Match the full name, not just the command line prefix","optionName":"-f"},"-g":{"type":"string","name":"Terminate processes based on process group ID","optionName":"-g"},"-i":{"type":"check","name":"Ignore case sensitivity","optionName":"-i"},"-o":{"type":"string","name":"Filter processes based on specified user ID","optionName":"-o"},"-P":{"type":"string","name":"Terminate processes based on specified process ID","optionName":"-P"},"-n":{"type":"check","name":"Same as pgrep, only print pid without sending signal","optionName":"-n"},"-s":{"type":"string","name":"Terminate processes based on session ID","optionName":"-s"},"-t":{"type":"string","name":"Terminate processes based on terminal","optionName":"-t"},"-u":{"type":"string","name":"Terminate processes based on user ID","optionName":"-u"},"-v":{"type":"check","name":"Exclude information related to processes","optionName":"-v"},"-x":{"type":"check","name":"Same as pgrep, match the entire process name instead of just the command line prefix","optionName":"-x"},"-e":{"type":"string","name":"Terminate processes based on process name","optionName":"-e"}}}],"content":"<p>The <code>pkill</code> and <code>kill</code> commands are both tools used to terminate processes, but they differ in the following ways:</p>\n<ul>\n<li>The <code>kill</code> command requires you to manually specify the PID of the process to be terminated, while the <code>pkill</code> command can terminate a process based on its name or other attributes.</li>\n<li>The <code>kill</code> command can only terminate a process, while the <code>pkill</code> command can also display process information and list process IDs.</li>\n</ul>\n<p>Therefore, if you know the PID of the process, you can use the <code>kill</code> command to terminate it. If you only know the name or other attributes of the process, then using the <code>pkill</code> command may be more convenient.</p>\n<h2 id=\"examples\">Examples</h2>\n<ul>\n<li><p>Kill a process based on its name:</p>\n<pre><code>pkill nginx\n</code></pre>\n</li>\n<li><p>Kill a process based on its name for a specific user:</p>\n<pre><code>pkill -u user nginx\n</code></pre>\n</li>\n<li><p>Kill a process based on its name for a specific process group ID:</p>\n<pre><code>pkill -g 123 nginx\n</code></pre>\n</li>\n<li><p>Kill a process based on its name for a specific session ID:</p>\n<pre><code>pkill -s 1234 nginx\n</code></pre>\n</li>\n<li><p>Kill a process based on its name for a specific terminal:</p>\n<pre><code>pkill -t pts/0 nginx\n</code></pre>\n</li>\n<li><p>Kill a process based on its name for a specific process ID:</p>\n<pre><code>pkill -P 12345 nginx\n</code></pre>\n</li>\n<li><p>Kill all processes:</p>\n<pre><code>pkill -9 -e -f .\n</code></pre>\n</li>\n</ul>\n"}
{
  "name": "cmd-generators",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node to-json.js"
  },
  "author": "phphe",
  "license": "MIT",
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
